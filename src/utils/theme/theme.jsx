import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1C1C1C",
    },
    secondary: {
      main: "#C20457",
    },
  },

});

export default theme